#lang racket
(define (einstellige-quersumme zahl)
  (iter zahl 0))
(define (iter zahl summe)
  (if (= zahl 0)
      summe
      (iter (quotient zahl 10) (+ (remainder zahl 10) summe))))

(einstellige-quersumme 123)

(einstellige-quersumme 346)



;Aufgabe2

(define (vergleich zahl op)
  (